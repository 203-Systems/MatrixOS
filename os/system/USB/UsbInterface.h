#pragma once

#include <cstdint>
#include <string>

// enum class InterfaceType : uint8_t { Vendor = 0, CDC = 1, MSC = 2, HID = 3, MIDI = 4, Audio = 5, DFU = 6, Invalid = 0xFF };

class UsbInterface {
};